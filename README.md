AI-powered self-serve analytics platform where users upload any CSV and query it in natural language. Ingests data into HDFS, cleans via PySpark, creates Hive tables with auto schema inference and relationship discovery. NL-to-SQL engine using ML intent classifier + rule-based SQL generator. Delivers visualizations and insights.

